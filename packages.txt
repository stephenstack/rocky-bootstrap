# Core packages installed by scripts/base.sh on every host.
# One package per line. Lines starting with # are ignored.

# --- shell + editing ---
vim-enhanced
nano
bash-completion
tmux

# --- networking + diagnostics ---
curl
wget
bind-utils
net-tools
nmap-ncat
tcpdump
traceroute
mtr

# --- system tools ---
htop
iotop
lsof
strace
sysstat
psmisc
which

# --- archive / transfer ---
tar
unzip
zip
rsync

# --- security / firewall ---
firewalld
fail2ban
policycoreutils-python-utils

# --- build basics ---
# Note: epel-release + dnf-plugins-core are handled by step_install_epel
# in scripts/base.sh — they must be installed before EPEL packages
# (htop, fail2ban, btop, fzf, jq, ripgrep) can resolve.
git
make

# --- chrony for time sync (Rocky ships it but make it explicit) ---
chrony
